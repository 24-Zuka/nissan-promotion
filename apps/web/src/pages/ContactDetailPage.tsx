/**
 * 顧客詳細。  [担当: Sub D — UI / 仕様3.2]
 * - 顧客情報(必須/任意項目)・車両リスト・直近メモ・未完タスク・次回フォロー予定を表示。
 * - 「メモ追加」: api.createNote（body.task で次アクションも同時登録＝時短UI・仕様3.3）。
 * - 「タスク追加」: api.createTask。
 * - 「車両追加」: api.createVehicle（generate_maintenance チェックでメンテ自動生成）。
 */
export default function ContactDetailPage() {
  return <div className="p-6">ContactDetailPage — [Sub D 実装予定]</div>;
}
