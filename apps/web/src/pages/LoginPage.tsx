/**
 * ログイン画面。  [担当: Sub D — UI]
 * - username + PIN(初期 1206) を入力 → useAuth().login → 成功で "/" へ遷移。
 * - 失敗時はメッセージ表示（理由は区別しない）。テンキー風UIが望ましい（iPhone）。
 */
export default function LoginPage() {
  return <div className="p-6">LoginPage — [Sub D 実装予定]</div>;
}
