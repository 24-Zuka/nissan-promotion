/**
 * 顧客一覧。  [担当: Sub D — UI]
 * - api.listContacts()。ランク順 → 氏名。新規作成(api.createContact)モーダル/画面。
 * - 行タップで /contacts/:id へ。
 */
export default function ContactsPage() {
  return <div className="p-6">ContactsPage — [Sub D 実装予定]</div>;
}
